# 実装計画: 特典航空券空席判定ロジックのダミーコード完全撤去と100%精度化

`united_monitor.py` 内に残存していた「特定期間の全日付・全土日を強制的に空席あり（True）にするダミー判定コード」を1文字残さず完全除去し、実際に通信して得られた本物の空席データのみを評価・通知する100%精度のエンジンへ修復します。

## ユーザーレビュー必須事項

> [!IMPORTANT]
> 修正後、これまで944件等と過剰に誤検出されていた空席件数は、実際に現在予約可能なリアル空席数（数十件〜0件程度）に急減します。これが本来の正常な仕様・通知結果となります。

## 変更内容

### `united_monitor.py`

#### [MODIFY] [united_monitor.py](file:///Users/katsupapa_1/.gemini/antigravity/scratch/united-award-monitor/united_monitor.py)

1. **`fetch_ana_route_availability_12months` の修正**
   - API取得処理（`traverse(data)`）で抽出された `OK`, `VACANT`, `AVAILABLE` などの実際の空席ステータスのみを `month_map` に登録。
   - 後半に残存していた「2026年〜2027年3月の全日付を無条件で `month_map[d_fmt] = True` に上書きするブロック（L414-L430）」を完全削除。

2. **`fetch_solaseed_route_availability_12months` の修正**
   - 単純に全土日を `True` と判定していたダミーコード（L445-L470）を撤去。
   - ANA/ユナイテッドのAPIでソラシドコードシェア便枠として確認できた空席、または実際の照会結果のみを正しく反映するクリーンロジックに差し替え。

3. **予約発売開始前（ダイヤ未確定期間）および受信用枠の制限**
   - ソラシドおよびユナイテッドの実際の予約発売上限日数を超える未来日付を確実にフィルタリング。

---

## 検証計画

### 自動・手動テスト
1. **ローカルテスト実行**:
   `python united_monitor.py` を手動実行し、空席判定結果から未発売期間や偽土日枠が排除されていることを確認。
2. **件数の検証**:
   全944件というダミー数字ではなく、実際に空席がある日付・路線のみが厳格に検出されていることをコンソール出力で確認。
3. **GitHub Push**:
   mainブランチへPushし、Actionsの次回定期スキャンで正常に動作することを確認。
